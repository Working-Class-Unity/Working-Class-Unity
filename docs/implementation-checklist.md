# Implementation Checklist Audit

Audit date: July 10, 2026; amended through July 28, 2026.

This file maps the guide's Step 20 checklist to current repository evidence. Items that require third-party account action are not marked as locally complete unless the repository contains a concrete implementation or runbook.

## Verification Evidence

- `.nvmrc`, package engines, and all Docker stages declare Node 24; `packageManager` and `engines.pnpm` declare pnpm 11.1.2, and `check:toolchain` verifies their agreement.
- `npm run bootstrap` and `npm run verify:pinned` provide the frozen-install and full-gate entry points without requiring Corepack or a global pnpm install.
- `pnpm run check` passes.
- `pnpm run stylelint` parses global CSS and Vue styles with pinned standard configurations, requires every ordinary SFC rule to be in `@layer components`, rejects `!important`, and is included in `pnpm run check`.
- The retired CSS regex checker has no replacement source assertion. The existing browser journey owns rendered skip/focus/current navigation, 44px representative targets, focus/control/text contrast, 320px Chromium-viewport and 200% root-font reflow, reduced motion, Axe, overflow, and console evidence. These automated cases are not physical-device, browser-zoom, or complete accessibility certification.
- `pnpm run api:smoke` self-builds, migrates, and starts a production server on loopback with fresh disposable database/provider state; it exercises representative identity, family-plan, project, local Files, and locally signed Stripe packaging behavior before cleanup. Its single billing check owns packaged route/config/raw-body/official-signature composition and duplicate receipt idempotency; focused Vitest owns the billing behavior matrix.
- `pnpm run ops:smoke -- --base-url=<url>` is the only production-targetable smoke. Its credential-free `GET` checks derive module expectations dynamically and cover the shell, public liveness, baseline security headers, and representative anonymous boundaries.
- `pnpm run worker` runs and exits cleanly when no jobs are queued.
- `pnpm run db:backup` writes a timestamped SQLite backup for the local database and reports both structural integrity and foreign-key results.
- `pnpm run db:migrate:check` applies the four-entry package—preserved `0000_pre_release_baseline.sql`/`0001_runtime_invariants.sql` plus #169 `0002_stripe_subscription_persistence.sql`/`0003_stripe_subscription_invariants.sql`—to temporary SQLite and verifies all 30 triggers. Maintenance and focused migration behavior prove fresh and repeat application, exact-prefix upgrade, legacy reconciliation, transactional failure/rollback, corrected retry, integrity, foreign keys, exact current-ledger/schema identity, and rejection of foreign, drifted, empty, or incomplete state without mutation.
- `pnpm run build` passes.
- `pnpm run ops:readiness` and strict mode share the built server's evaluator, require the database/readiness/auth/application values plus one complete core email transport, six exact module flags, and the independent Google flag; they add provider requirements for enabled optional modules and require Google credentials only when its exact flag is `true`.
- `pnpm run ops:readiness -- --env-file=.env.production.example` reads the committed production template and reports the expected unresolved real-service values.
- External completion requires a filled production-evidence copy with an accountable human reviewer and review date; repository Markdown is not an executable certification format.
- `pnpm run doctor` verifies only the effective Git-ignore protection for representative local environment, SQLite, log, and filled production-evidence artifacts. ESLint owns the object-storage provider import boundary; executable toolchain, build, migration, typecheck, and test commands own their respective requirements.
- R-006/R-007 built-runtime evidence rejects incomplete, malformed, poisoned, or unsupported configurations before listen, proves canonical build canaries do not enter output/logs, and verifies runtime-only core/Stripe config. Focused worker-entry evidence owns the standalone disabled no-database boundary and the real Files handler mapping against migrated disposable SQLite.
- The R-007 runtime-policy manifest covers six flags, every conditional provider requirement, exclusive API/UI routes, and readiness semantics. Missing/malformed/mismatched flags are incomplete; exact false is healthy-disabled; and complete enabled modules are locally ready. Direct behavior tests own service/provider boundaries; exhaustive file, dependency, integration, and test-scope inventories were retired by CI-S02A2.
- Disabled-state evidence proves stable 404 `MODULE_DISABLED` before auth/body/I/O, exact-prefix collision safety, stale credentials with zero provider calls, no Turnstile/Sentry initialization, a Jobs-disabled no-DB worker exit, and Jobs-ready/Files-disabled cleanup remaining queued without consuming an attempt or touching storage. R-023B independently owns effective standard response headers and CSP.
- R-023B pins `nuxt-security@2.6.0` as the sole owner of standard browser-security headers, enforced deny-by-default CSP, per-response nonces, emitted-asset SRI, and `X-Powered-By` removal, and deletes the parallel custom header middleware. Its generic CORS, CSRF, request-size, rate-limit, XSS-body, and method middleware is disabled because Better Auth or application-specific controls own stricter guarantees. Focused Vitest owns evaluated configuration/provider sources, Playwright owns final CSP/nonces/blocking/hydration/SRI, the invitation journey owns `no-referrer`, deployment smoke owns effective header presence, and one packaged start uniquely proves runtime policy overrides fail before listen.
- Ordinary Vitest uses a small terminal-state guard for skip, todo, expected-failure, missing, and nonpassing cases. Coverage is an ad hoc diagnostic rather than a completion threshold.
- At the R-008A boundary, built-runtime evidence rejected three Better Auth client fallbacks before build and 21 incomplete, malformed, default, insecure, or unsupported configurations before listen; ran 15 auth/security checks including authenticated `/auth` and `/billing` SSR; and proved bounded telemetry, cookie, origin, callback-URL, proxy, client-IP, and rate-limit behavior against disposable state. CI-R08A later moves the three pre-build config imports to independent evaluated Nuxt-config Vitest behavior while retaining the distinct packaged startup and production-build evidence.
- R-008B adds one fail-closed origin policy for every unsafe app-owned `/api` command. Focused evidence covers exact Origin, same-origin Fetch Metadata, Referer fallback, missing/malformed/conflicting signals, workspace/project/file/billing/account-lifecycle family fixtures, exact exemptions and neighboring paths, redacted `403 CROSS_ORIGIN_REQUEST_BLOCKED`, router-equivalent encoded-path handling, and module-before-origin ordering. One built `/%61pi/projects` canary owns only packaged decoded-path/middleware registration, exact error/privacy/security headers, and no-write rejection. Focused real-H3 and service behavior now composes `POST /api/webhooks/stripe`, official `stripe@22.3.1` signature verification, the full migration package, and temporary SQLite to own exact raw bytes, invalid-signature/no-write, event idempotency/ordering, reconciliation, and minimized persistence. Deterministic local signatures are not Stripe sandbox certification. Read-only workspace resolution, user-owned project CRUD, family billing, and account lifecycle are live. Project authorization remains an independent authenticated-user/resource predicate. The policy is based on the [OWASP CSRF guidance](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html), [W3C Fetch Metadata](https://www.w3.org/TR/fetch-metadata/), [Better Auth security contract](https://better-auth.com/docs/reference/security), and [Stripe signature contract](https://docs.stripe.com/webhooks/signature); its stricter signal-agreement rule and application `403` are app-owned.
- R-009 built-runtime evidence rejects 25 invalid configurations before listen; proves exact public `204` liveness, protected redacted `200`/`401`/`503` readiness, and no readiness secret in browser hydration; and adds a real-container `healthy` to dependency-failed `unhealthy` CI transition while liveness remains available. Deployment orchestration runs and verifies maintenance before application startup; `/api/ready` itself proves current database availability, not migration-ledger or schema identity.
- R-010 adds a deny-by-default Docker context, nested/root canary plus image configuration/history proof, `node:node` runtime, and a same-image Drizzle/SQLite migration-backup-verify-restore job. ADR 0015/#151 established successful fresh/repeat initialization from the preserved two-entry starting prefix, exact ledger/schema verification, injected failure rollback, atomic private backup, symlink/path confinement, held-writer and corruption behavior, staged restore with session/one-time-verification invalidation, complete journal/WAL/SHM state, and replacement rollback. #169 extends that evidence target to fresh/repeat/upgrade behavior through four entries and exactly 30 triggers. The real container worker uses a fresh named `/app/data` volume for network-disabled maintenance jobs, fail-closed cases, non-root container generations, database/local-object/backup replacement persistence, and the retained readiness failure transition. R-031/#35 adds the separate bundled R2 operator, exact copied-snapshot verification, conditional Content-MD5 publication, full remote SHA-256 read-back, deterministic retry/interruption/path/lock coverage, active-local-Files refusal, verified fetch, six-hour/12-hour/30-day-lock/35-day-lifecycle/monthly-drill policy, and no RPO/RTO promise. #36 provisions the real bucket/policies, Coolify runner/volume, private off-host receipt sink, independently scheduled dead-man monitor, and release topology; #37 creates and configures the task and notifications, then executes and certifies provider, failure/silence/freshness, and isolated-restore behavior.
- R-011 makes `ops:smoke` structurally read-only and proves its built-runtime execution leaves the same SQLite connection's [`PRAGMA data_version`](https://www.sqlite.org/pragma.html#pragma_data_version) and the local-provider-directory fingerprint unchanged. The separate Full integration worker refuses targets, inherited app/provider configuration, and live credentials before creating state; owns a fresh named SQLite database and runtime working directory (not an app-owned workspace); uses local Files plus a locally signed webhook fixture; and cleans on ordinary success or failure. Catchable-signal cleanup remains implemented but is not exercised through the actual runner; CI-S02B/#70 explicitly retires `INT-01D`. Real provider [sandbox](https://docs.stripe.com/sandboxes) certification remains external staging evidence.
- R-014 adds the provider-neutral capture/SMTP email service and Better Auth magic-link server/client plugins with database verification, hashed five-minute storage, five-per-60-second request/redemption limits, safe return paths, and password UI/API removal. Focused evidence covers capture permissions/publication, SMTP option/success/rejection/failure, incomplete configuration, production capture rejection, and the pinned auth options. Production-composed HTTP cases through `createAuthentication`, the public handler, the configured Drizzle adapter, current migrations, and temporary SQLite own storage, atomic consumption/replay/expiry, neutral issuance, generic failure, route absence, session/cookie policy, and trusted-client rate limits. The selected SQLite claim is atomic only at the verification-row `DELETE ... RETURNING` gate; it is not a transaction spanning user/session creation. R-020A implements the app-owned `/login`, `/signup`, and `/account` routes around that same authentication system, and R-033 retains real hosted-email certification.
- R-015 adds independently configurable Google authorization-code identity with exact `openid email` scopes, verified same-email implicit linking, a nameless-profile fallback, database state/PKCE, token non-retention, fixed provider-log redaction, token/profile route removal, exact-account list/unlink UI with a fresh-session boundary, invalid/replayed-code cleanup, and deletion cascade evidence. Disabled Google starts without credentials or UI/state work; enabled-incomplete configuration fails before listen. R-015 raised the built worker to 33 pre-listen cases and 19 auth/security checks. No schema migration is added. Local unlink is not Google grant revocation; R-033 retains real consent/callback/denial/removal/relink/revocation certification.
- R-016/R-017 establish the current Better Auth Organization `1.6.23` field and capability model. The clean baseline creates the approved Organization schema and provisions personal organizations atomically from the custom SQLite user-insert trigger. The synchronous `better-sqlite3` driver leaves Better Auth's incompatible async transaction wrapper disabled. Family-plan billing and caller-only self-leave derive current persisted organization membership independently of non-authoritative `activeOrganizationId`; private resources remain user-owned. R-018 owns lifecycle; R-020B owns the personal `/app` shell; R-020A owns the public identity, authenticated account, legal-template, and global-error routes; R-021 implements the Reka account/family menu; and R-022 implements the private project route journey.
- R-019 is preserved history for the secured organization-owned project boundary, and R-019C/#84 is preserved history for the correction back to user ownership. R-019D/#123 supersedes their active migration and slug claims: the clean baseline creates projects with restrictive `owner_user_id`, no slug column or slug index, and duplicate names allowed; project forms and DTOs are name-only; and immutable project IDs address records. Current focused HTTP and SQLite behavior proves same-plan cross-user isolation, active-organization/membership irrelevance, concealment, concurrency, fresh/repeat initialization, constraints, integrity, and foreign keys. R-029S-R/#140 removes the derived Search surface and verifies its final schema objects are absent. No compatibility migration, source-text checker, or generic ACL is added.
- R-017I adds the privacy-minimized invitation slice without enabling Better Auth's native invitation HTTP surface. Truthful adapter delivery and same-ID resend, verified-recipient concealment, expiry/replay/rejection/cancellation/limits, concurrent accept-vs-accept/reject/cancel, fixed projections, and immediate membership/loss are exercised through Better Auth's public server API and the configured Drizzle adapter on disposable SQLite. The non-mutating `/invite/:id` page preserves only its exact anchored return path, requires explicit POST actions, and applies private/no-store plus no-referrer behavior. Acceptance may update `activeOrganizationId`, but authorization immediately re-reads persisted membership. #169 proves the app-owned repair when the pinned `transaction: false` path inserts the member before a later active-session failure, and moves app-owned reject/cancel authority to pending-to-terminal compare-and-set updates inside `BEGIN IMMEDIATE`. Process-local queues still order shared-connection async work and same-ID resend delivery in the one-replica topology; terminal membership and capacity correctness use SQLite transactions, revisions, and constraints. R-024A historically added a six-accepted-person guard without counting pending invitations; #169 supersedes that capacity rule so each pending unexpired invitation reserves one of the five non-manager seats. R-033 owns hosted delivery.
- R-018A/#78 narrows that slice to the owner/member family-plan model. Each user owns exactly one marked, normally invisible organization; an independent owner may list, create, ID-bound resend, or cancel member invitations through `/api/invitations`, while a member of someone else's group cannot operate either the joined group or their dormant marked group. New requests accept only email, every pending/live invitee role is `member`, recipient projections are role-free, and that tranche had no member-directory, admin, role-mutation, transfer, removal of another member, or public organization-delete surface. R-024A2 added caller self-leave at `POST /api/account/family/leave`; #169 retains self-leave and adds only a narrow billing-current manager command that removes one non-manager by opaque immutable reference. Owners still cannot leave or transfer. The generated owner/member check and custom baseline triggers create that invariant directly, and real-database behavior verifies it. Focused auth/Organization/Drizzle behavior—not a new source-text checker—remains the primary evidence.
- R-018C/#80 implemented the only public destructive lifecycle command at `DELETE /api/account`. Exact confirmation and a Better Auth session younger than 24 hours entered one synchronous SQLite `beforeDelete` transaction; native Better Auth/Organization deletion paths remained unavailable. In that completed tranche, the transaction deleted caller private/auth/family-plan state, preserved invitees, committed the existing delayed file-orphan cleanup, and detached only minimized justified provider continuity without Stripe I/O. Final generated baseline `0000` created the identity-free detached-record constraints and provider event ordering directly. Focused Better Auth/SQLite cases proved confirmation/freshness, preservation, minimization, exact and raced late events without invented history, rollback/retry, concurrency, lost response, re-registration, late-write/orphan rejection, object cleanup, file-completion preservation, and leased-job recovery. Maintenance behavior included incomplete-internal-prefix and exact-current-ledger privacy-schema forgery rejection. This is historical pre-#169 evidence, not proof of the amended cancellation-before-deletion contract.
- ADR 0015/#151 establishes the preserved starting point: generated relational `0000_pre_release_baseline.sql` creates the prior complete schema without Search/FTS objects, and custom `0001_runtime_invariants.sql` installs the original 16 family/billing authority triggers. #169 adds generated `0002_stripe_subscription_persistence.sql` plus custom `0003_stripe_subscription_invariants.sql`, with a four-entry final ledger and exactly 30 triggers. Fresh, repeat, exact-prefix upgrade, transactional failure/rollback, and corrected retry are required.
- R-020C/#116 composes those existing invitation and deletion APIs into `/account` without changing server authority, schema, providers, or the CSS foundation. The family section sends only email or an opaque invitation ID, renders only pending email/expiry summaries, refreshes after delivery failure, and never infers authority from joined membership or `activeOrganizationId`. The destructive section requires exact `DELETE`, distinguishes stale-session reauthentication from an uncertain failed response, and clears local session state only after confirmed success. Its scoped styles live solely in the existing `components` layer.
- R-021/#23 pins `reka-ui@2.10.1` and uses direct named imports in one app-owned account menu. Account remains a link, Sign out remains a command, App navigation remains native, and no workspace selection, slug, role, capability, or member-management state is rendered or hydrated. The retired `/billing` link remains absent; `/account` conditionally links to `/account/billing` only when Billing is ready and reveals Family controls only after manager authority is confirmed. The existing Playwright account journey owns pointer, keyboard, dismissal/focus, Axe, narrow-viewport/root-font reflow, and sign-out failure/retry behavior; no Nuxt module, component kit, runner, or source assertion is added.
- R-022/#30 implements `/app/projects` and `/app/projects/[projectId]` with native/component-layer controls. The pages authenticate before fetching, submit only names or immutable IDs, allow duplicate names, share one concealed view for malformed/missing/deleted/foreign records, keep retryable failures distinct, clear stale project data on session loss, and do not navigate after deletion until success is confirmed. App is current only at exact `/app`; Projects is current for collection and detail. Focused HTTP/Vitest remains primary for auth ordering, ownership, same-family isolation, duplicate names, concealment, and `activeOrganizationId` irrelevance. Route-controlled Playwright owns rendered states, request shapes, keyboard/focus, inline deletion, narrow Chromium viewport/root-font stress, Axe, overflow, and clean observations. One desktop real-login path performs CRUD through built Nitro and migrated temporary SQLite. No UI kit, custom runner, or source assertion is added.
- R-024A/#126 pinned `stripe@22.3.1` and implemented the original backend family-plan authority under `/api/account/billing` plus `POST /api/webhooks/stripe`. Server-owned Price and quantity `1`, durable Checkout attempts, organization-owned customers/current snapshots, owner reauthorization around provider I/O, paginated reconciliation with full-revision conflict detection, receipt-last idempotency, ordering, and minimized no-payload persistence were covered by focused service/HTTP/real-SQLite behavior. A membership backed by an unambiguous `active`, `trialing`, or `past_due` snapshot granted paid capability without granting private-data access. R-024A2/#129 gave each person exactly one commercial relationship—independent, manager, or covered member. Accepted members could not Checkout, open Portal, reconcile, change cadence, or cancel through their personal group even when inherited access was inactive; strict invitation admission and concurrent database guards prevented parallel relationships/subscriptions; and core `POST /api/account/family/leave` removed only the caller's external membership. R-024B/#127 rendered the original `Family plan` relationship at `/account/billing`. That tranche's immediate deletion made no Stripe call and retained only identity-free continuity. This paragraph remains historical local evidence; #169 supersedes its catalog and deletion statements, and #37 still owns sandbox/live provider certification.
- #169's approved current contract adds one Product with five distinct Personal weekly/monthly/annual and Family monthly/annual Prices plus one explicit Portal configuration. It adds application-owned transitions, the exact 21-event webhook target, 14-day-bounded dunning, pending-invitation seat reservations, Personal residual state, manager removal, and Family dissolution. Billing now requires Jobs and exactly one supervised same-image, same-volume worker. For a billing owner, a fresh exact `DELETE` must durably record and synchronously attempt immediate no-refund/no-proration subscription cancellation, retrieve and confirm cancellation, and only then enter local deletion. Ambiguity retains the user and private state; the worker may retry cancellation/retrieval only and can never delete a user. Worker confirmation still requires the person to return with another fresh exact `DELETE`. Local behavior, stop/restart safety, retries, backlog/deadline monitoring, and the no-worker-deletion boundary are implemented and deterministically verified; staging sandbox evidence remains external under #37.
- R-025/#32 completes Files as an optional private-resource lifecycle with explicit disabled/incomplete/ready plus local/R2 driver behavior. Every route authenticates and predicates immutable file ID plus persisted owner ID; foreign, deleted, and unknown resources, including same-family users, share a `404`. New 1-byte-through-25-MiB uploads require canonical `Content-MD5`, opaque keys, and a 15-minute capability. Local bytes stream to contained temporary storage before verified publication. Exact AWS SDK fixtures verify Cloudflare R2 PUT/HEAD/GET signing, checksum configuration, endpoint validation, timeouts/retries, bounded opaque-token pagination, partial delete failure, and delayed replay-safe cleanup without a provider account. Files requires Jobs; account deletion commits empty delayed cleanup and never waits for storage I/O. Ordinary expiry-plus-one-minute jobs remain. After binding initialization, the Files-ready worker's throttled pre-claim SQLite `IMMEDIATE` check retains a viable full-root sweep on a 24-hour safety cadence despite continuous or non-root/cursor work, so a provider upload completing after the ordinary sweep still converges without an assumed completion bound. The persisted binding includes driver, bucket, and normalized R2 account/jurisdiction endpoint. Final generated baseline `0000` creates the settled Files integrity, expiry, lifecycle, and bounded-query schema directly. There is no Files UI, Turnstile coupling, generic ACL, public bucket, multipart/TUS layer, custom signer, or second queue. #37 owns real R2 token/CORS/browser/provider/deletion certification.
- R-026/#33 completes AI as an optional private text-conversation boundary with disabled/incomplete/ready behavior and no UI/provider account. Exact `openai@6.47.0` calls Responses directly through a thin server adapter with one allowlisted `gpt-5.6-luna`, locally authoritative visible history, `store: false`, explicit no-cache mode, no provider conversation state/streaming, low reasoning, 4,096 output tokens, 60-second timeout, zero SDK retries, and SDK logging off. Every route authenticates and predicates immutable conversation ID plus persisted owner ID; foreign/deleted/unknown and same-family IDs share `404`. SQLite owns 100 conversations, 50 reserved attempts/day, one active generation, idempotency, safe attempts, history revision, and clear/delete/account-deletion behavior. Provider envelopes/content never enter ordinary logs or Sentry. R-026F/#148 adds private subordinate File Search with one configured deployment-owned read-only corpus, ten results, title-only citations, and a separate bounded operator workflow. #149 adds private subordinate Web Search with a strict one-to-100-domain server allowlist, medium context, and durable bounded HTTPS title/URL/span citations. Both default off; enabled tools use automatic choice, no parallel calls, and one total built-in call, so a combined turn uses File or Web Search, not both. Raw queries, actions, complete sources/results, and envelopes are not requested/persisted/logged. Deterministic fakes make no key/network call or charge. Final generated baseline `0000` persists both citation kinds directly. `store: false` is not Zero Data Retention, does not delete persistent Files/vector stores, and live Web Search is not HIPAA/BAA-covered. #37 owns live project/resource isolation, runtime/operator permissions, model/tool/domain/citations, API/search/storage billing/spend, provider request/error/rate/pagination, retention/ZDR/MAM/HIPAA, third-party behavior, deployed logging, and user/corpus deletion certification. There is no AI Gateway, generic provider/RAG/tool framework, embeddings/vector database, UI, streaming, arbitrary agents, attachments, user-owned corpus, application crawler/index, sharing, or generic ACL.
- The current browser verification discovers the same logical tests in required Chromium projects with desktop `1280×900` and narrow `390×844` defaults. The route-controlled project matrix plus Billing's independent, manager, reconciliation, and session-loss slices run once in desktop Chromium. Billing's member presentation runs in both projects; the mobile-project branch additionally sets `390×844`, `320×800`, and 200% root-font conditions, avoiding duplicate command evidence while retaining narrow-state coverage. The remaining journeys cover identity/account/legal/error, Google-enabled route doubles, invitations, and deletion; `/auth` and the retired `/billing` page remain absent. #169 browser evidence covers all-five-offering presentation, transitions/dunning/reconciliation states, accepted/reserved seats, manager removal, cancellation-pending deletion, relationship-specific controls, truthful Checkout-return state, session loss, exact command bodies, disabled concealment, Axe, responsive layout, overflow, and absence of provider identifiers without calling Stripe. Residual Personal restoration remains focused server behavior because it is a persistence and entitlement transition rather than a distinct browser contract. The manager surface renders only display name and email; its opaque immutable reference is undisplayed and used only in the removal request. The bearer-link journey disables trace, screenshot, and video. The launcher registers private capture paths and values before scanning bounded output/artifacts; unreadable, overflowing, or leaking observations fail closed and diagnostics are redacted. Each Axe call uses the unfiltered rendered page and requires no returned violations; it does not replace manual accessibility review. Focused server tests remain primary for authentication, authorization, persistence, provider ordering, and data-loss boundaries; browser doubles do not claim sandbox/live certification.

Known warnings:

- Nuxt/AWS dependency builds emit Node `DEP0155` warnings, but builds complete.

## Phase Status

| Phase                     | Status                                                                                                | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Repository and app shell  | Complete locally                                                                                      | pnpm workspace, Nuxt app in `apps/web`, root scripts, `.env.example`, README, dev server, and production build are exercised by their direct commands; Doctor protects private local artifacts from ordinary Git addition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Frontend foundation       | CSS/accessibility, account actions, Reka menu, and project journey complete locally                   | Ordered reset/tokens/base/layout/components/utilities layers, semantic tokens, shared page/status/form primitives, skip/main/current navigation, responsive browser evidence, ESLint, maintained Stylelint policy, the app-owned `reka-ui@2.10.1` account/family menu, and private project collection/detail pages are present. Family invitation, destructive-account, menu, and project styles extend only the component layer; primary navigation and project controls remain native.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Server foundation         | Complete locally                                                                                      | Minimal public liveness, protected redacted readiness, utility folders, Zod/h3 validation pattern, structured errors, auth/session helpers, `nuxt-security@2.6.0` ownership of standard headers/enforced CSP/nonces/SRI, numbered module-before-origin application middleware, a default-deny same-origin boundary for unsafe app API commands, caller-only workspace GET resolvers, and request-level smoke checks are present. Generic module features remain disabled where Better Auth or application-specific controls are stricter; the origin gate is not authentication or resource authorization.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Data layer                | #169 schema and forward migrations complete locally; deployment pending                               | Drizzle ORM/Kit, SQLite driver, schema index, app tables, preserved `0000`/`0001` plus #169 `0002`/`0003`, organization billing/current snapshot, transition/dunning/join/deletion state, accepted-plus-reserved capacity, reciprocal one-family authority, Files integrity/expiry constraints, AI owner/idempotency/history/quota/citation constraints, exactly 30 final triggers, no Search/FTS/cache objects, and immutable private-resource ownership form the current target. Fresh/repeat/exact-prefix upgrade, legacy reconciliation, transactional failure/rollback, and corrected retry pass locally; applying the package to persistent staging remains external.                                                                                                                                                                                                                                                                                                                                                                                              |
| Auth                      | #169 cancellation-before-deletion amendment complete locally; hosted-provider staging pending         | Better Auth, Nuxt handler/client plugin, database-backed hashed five-minute magic links, shared `/login` and `/signup` UI, authenticated `/account`, exact return/origin policy, password-route removal, provider-neutral capture/SMTP service, independently configured Google authorization, verified same-email linking, token non-retention, local unlink, atomic personal-organization provisioning, non-authoritative active selection, strict one-family owner/member authority, privacy-minimized member invitations, caller-only self-leave, six-person accepted-plus-reserved enforcement, immediate caller-only deletion, minimized hydrated identity, and the account/family action menu are present. #169 supersedes the earlier no-Stripe deletion path; its durable cancellation/reconfirmation behavior is implemented and verified locally. Hosted email, Google, and Stripe staging evidence remain unclaimed.                                                                                                                                         |
| Files and Cloudflare      | Local lifecycle complete; real R2 certification pending                                               | R-025 implements disabled/incomplete/ready local/R2 driver behavior, owner-only concealed routes, minimized metadata, opaque keys, 25-MiB `Content-MD5` uploads, persistent streamed local bytes, exact-SDK R2 PUT/HEAD/GET capabilities, the full persisted provider binding, bounded pagination/retry/cleanup, ordinary expiry jobs plus a deduplicated 24-hour root safety sweep, Jobs coupling, account-deletion convergence, and the Files schema now created by final generated baseline `0000`. There is no generic sharing; a fork adds feature-specific access explicitly. Real bucket/token/CORS/browser/provider/error/deletion evidence remains #37.                                                                                                                                                                                                                                                                                                                                                                                                         |
| AI conversations          | Local direct-OpenAI plus optional File/Web Search boundaries complete; provider certification pending | R-026/R-026F/#149 implement optional disabled/incomplete/ready AI plus nested disabled-by-default File and Web Search, authenticated owner-only concealed conversations/messages/citations, local history, server bounds/entitlement/quota/concurrency, idempotent single-dispatch attempts, clear/delete/account deletion, exact `openai@6.47.0` Responses requests, one deployment-owned read-only corpus, one server-owned Web allowlist, one total built-in call, separate bounded File/Web citations, separate corpus operator prepare/verify/delete, safe extraction/errors, no SDK retries/logging/cache/provider state, and deterministic fakes with no key, network call, or charge. Final generated baseline `0000` creates the local AI/citation schema. Real project/resource isolation, runtime/operator permissions, model/tool/domain/citations, API/search/storage billing and spend, provider request/error/rate/pagination, retention/ZDR/MAM/HIPAA, third-party behavior, no-content logging, and deployed user/corpus deletion evidence remains #37. |
| Payments                  | #169 Stripe-only expansion complete locally; provider certification pending                           | Official `stripe@22.3.1`, exactly five distinct Personal/Family Prices, explicit Portal configuration, organization customer/current snapshots, durable Checkout attempts, application-owned transitions, exact 21-event current-state handling, dunning, invitation reservations, residual Personal, minimized manager removal/dissolution, worker-backed effects, owner-only Portal/reconciliation, covered-member denial and self-leave, SDK webhook verification, minimized receipts, ordering/conflict fail-closed behavior, confirmed Stripe cancellation before local account deletion, and disabled operation are implemented and deterministically verified. Stripe Product/Price/Portal/webhook setup and provider evidence remain #37. No Apple App Store, Google Play, native-store build, or provider-neutral billing layer is implemented.                                                                                                                                                                                                                 |
| Deployment and visibility | Repo/image/runbook complete, live deployment external                                                 | Deny-by-default Docker context, canary image proof, non-root runtime, fresh named-volume replacement proof, same-image migration/backup/verify/restore job, bundled private-R2 operator, explicit backup/restore runbooks, `.env.production.example`, `ops:smoke`, protected readiness, Coolify/DigitalOcean docs, external-service checklist, human-reviewed production evidence template, Sentry config/test paths, and executable restore runbook are present. Creating the Droplet/Coolify resource, assigning the real volume, selecting the Git commit and recording its local image IDs, proving release ordering, and—when backup is selected—provisioning the private runner/bucket rules and certifying scheduled uploads/read-backs/restores still require external account access. Provider observability also remains external.                                                                                                                                                                                                                             |
| Mobile                    | Baseline complete, device testing external                                                            | Responsive browser verification and native-escalation guidance are present. Physical-device/browser verification remains staging work, and Capacitor is intentionally future work until product pressure appears.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| AI guardrails             | Complete locally                                                                                      | Dex task tracking, explicit task template, stable check commands, maintained Stylelint policy, browser verification rule, production feedback loop, and regression-test expectation are documented. Production Sentry/log review happens after an actual release.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |

## Remaining Non-Local Actions

- Under #36 and separate infrastructure authority, create and configure real Cloudflare DNS, WAF/rate-limit rules, optional Files R2 resources, and—when the deployment selects off-host backup—the separate private database-backup bucket/token/lock/lifecycle policy, backup runner, private off-host receipt sink, and independently scheduled dead-man monitor. Persistent baseline staging selects backup; forks may omit it and document the resulting recovery boundary. Under #37, create and configure the real task and notifications, then execute and certify scheduled upload, read-back, failure/silence/freshness behavior, and isolated restore for enabled backup.
- Create the one Stripe Product, five distinct Prices, and explicit reviewed Portal configuration; configure the exact 21-event webhook destination and Smart Retries; and prove all-five Checkout, transitions/dunning, seat reservations/manager removal, the Billing worker, and cancellation-before-deletion in the isolated sandbox.
- Create a DigitalOcean Droplet, install the reviewed Coolify version, create the Git-connected Compose app, configure the real `/app/data` volume and environment, certify its local-build → stop/remove → one-shot migration → start sequence in staging, deploy the selected commit to production, and record the resulting evidence.
- Confirm Sentry source maps and alerts against a real Sentry project.
- Create the transactional-email provider resource, verify the sending identity/domain, and record real delivery, rejection, bounce/suppression, expiry/replay, and operational evidence under R-033.
- Create the Google OAuth client and consent configuration, then record real consent, callback/denial, account-removal, relink, and Google-side revocation evidence under R-033.
- Create an isolated OpenAI API project/service-account key and record real model/tool/domain access, API/search/storage billing and spend limits, request/error/rate/timeout/citation behavior, retention/ZDR/MAM/HIPAA and third-party posture, no-content logging, and clear/delete/account/corpus-deletion evidence under R-033/#37 before enabling AI or either hosted search capability.
